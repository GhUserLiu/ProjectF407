# 查重系统依赖项
# Plagiarism Detection System Dependencies

# 核心依赖（必需）
python-docx>=0.8.11    # Word文档处理
openpyxl>=3.0.10       # Excel文件处理

# 中文分词（强烈推荐）
jieba>=0.42.1          # 中文精确分词，提升相似度检测准确性

# 语义检测（可选，增强改写检测）
sentence-transformers>=2.2.0  # 语义嵌入模型，检测改写抄袭

# 图片处理（可选）
Pillow>=9.0.0         # 图片相似度检测

# 其他可选依赖
numpy>=1.19.0         # 数值计算（sentence-transformers需要）
scikit-learn>=0.24.0  # 机器学习（可选）
