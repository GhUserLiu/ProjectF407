以下是利用VSCode批阅Keil单片机作业的可执行步骤清单，按从简单到复杂的顺序排列，方便你根据自身情况选择性实施。

---

## 🔧 准备工作：安装必要的工具

在开始之前，你需要在自己的电脑上安装以下工具：

| 工具 | 用途 | 安装方式 |
| :--- | :--- | :--- |
| **VSCode** | 批阅操作界面 | 官网下载安装 |
| **Python 3.6+** | 运行自动化脚本 | 官网下载，安装时勾选"Add to PATH" |
| **Keil MDK** | 调用其命令行编译 | 你电脑上已有的Keil |
| **Cppcheck** | 静态代码检查 | 官网下载，安装后添加到PATH环境变量 |
| **Ruby** | Ceedling单元测试框架依赖 | 官网下载安装 |

> **可选扩展**：在VSCode中安装 `Cppcheck` 插件（直接在扩展商店搜索安装）可以获得更好的静态检查体验。

---

## 📁 阶段一：最简单的起步——一键编译检查（15分钟可完成）

这个阶段让你能够**在不打开Keil的情况下，批量编译学生的Keil项目**，快速筛选出"编译不通过"的问题。

### 步骤1：确认Keil命令行可用

打开命令提示符（CMD），执行以下命令测试：

```cmd
# 调整为你自己电脑上Keil的安装路径
"C:\Keil_v5\UV4\UV4.exe" -?
```

如果看到帮助信息，说明命令行编译功能可用。

### 步骤2：编写批量编译脚本

创建一个名为 `batch_build.py` 的Python脚本：

```python
import os
import subprocess
import sys

# 你的Keil UV4路径
KEIL_UV4 = r"C:\Keil_v5\UV4\UV4.exe"

def build_keil_project(uvprojx_path):
    """编译单个Keil项目，返回 (成功/失败, 输出信息)"""
    cmd = f'"{KEIL_UV4}" -r "{uvprojx_path}" -j0'
    result = subprocess.run(cmd, shell=True, capture_output=True, text=True)
    # 检查输出中是否包含错误信息
    output = result.stdout + result.stderr
    has_error = "error" in output.lower() or result.returncode != 0
    return (not has_error, output)

def find_all_projects(root_dir):
    """递归查找所有.uvprojx文件"""
    projects = []
    for dirpath, dirnames, filenames in os.walk(root_dir):
        for f in filenames:
            if f.endswith('.uvprojx'):
                projects.append(os.path.join(dirpath, f))
    return projects

if __name__ == "__main__":
    # 修改为学生作业的根目录
    root_dir = r"C:\Student_Assignments"
    projects = find_all_projects(root_dir)
    
    results = {"success": [], "failed": []}
    for proj in projects:
        print(f"编译: {os.path.basename(proj)}")
        success, output = build_keil_project(proj)
        if success:
            results["success"].append(proj)
            print("  ✓ 编译通过")
        else:
            results["failed"].append(proj)
            print("  ✗ 编译失败")
            # 可选：将错误信息保存到文件
            with open(proj.replace('.uvprojx', '_build_log.txt'), 'w') as f:
                f.write(output)
    
    print(f"\n====== 批阅结果 ======")
    print(f"编译通过: {len(results['success'])} 份")
    print(f"编译失败: {len(results['failed'])} 份")
```

### 步骤3：运行脚本

```cmd
python batch_build.py
```

批量编译结果一目了然，编译失败的项目可以通过保存的日志文件快速定位错误类型。

> 📌 **评分建议**：编译通过即可获得基础分（建议占作业总分的10-15%）。

---

## 📁 阶段二：进阶——抽取源码进行深度检查（1天可完成搭建）

这个阶段让你能够**对学生的代码进行静态规范和核心逻辑验证**，无需依赖学生的Keil项目结构。

### 步骤1：从Keil项目中抽取源码信息

创建一个 `extract_project_info.py` 脚本，用于解析Keil的 `.uvprojx` 文件（本质是XML）：

```python
import xml.etree.ElementTree as ET

def parse_uvprojx(uvprojx_path):
    """从Keil项目中提取：源文件列表、头文件路径、预定义宏、芯片型号"""
    tree = ET.parse(uvprojx_path)
    root = tree.getroot()
    
    # 查找Targets → Target → TargetName
    target_elem = root.find('.//Targets/Target')
    target_name = target_elem.find('TargetName').text if target_elem else "unknown"
    
    # 提取源文件（需要根据实际XML结构调整XPath）
    src_files = []
    for elem in root.findall('.//File/FileName'):
        src_files.append(elem.text)
    
    # 提取包含路径
    inc_paths = []
    for elem in root.findall('.//IncludePath'):
        inc_paths.extend(elem.text.split(';'))
    
    return {
        "target": target_name,
        "src_files": src_files,
        "inc_paths": [p for p in inc_paths if p.strip()],
        "src_count": len(src_files)
    }

# 使用示例
if __name__ == "__main__":
    info = parse_uvprojx(r"C:\Student\project.uvprojx")
    print(f"项目名称: {info['target']}")
    print(f"源文件数量: {info['src_count']}")
    print(f"包含路径: {info['inc_paths'][:3]}...")  # 预览前3条
```

> 💡 **替代方案**：如果不想自己写解析脚本，可以在GitHub上搜索以下现成的开源工具：
> - **keil2makefile.py**：一键将`.uvprojx`转换为GCC+Makefile工程
> - **project-generator**：ARM官方开源工具，支持YAML配置，可生成Makefile、Keil、IAR等多种工程

### 步骤2：运行Cppcheck静态代码检查

在VSCode中创建一个任务（`.vscode/tasks.json`），对**提取出的源文件**进行检查：

```json
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "static-check",
            "type": "shell",
            "command": "cppcheck",
            "args": [
                "--enable=all",
                "--std=c11",
                "--quiet",
                "--template={file}:{line}:{severity}:{id}:{message}",
                "--output-file=./report.txt",
                "./src"  // 指向学生源文件所在目录
            ],
            "problemMatcher": {
                "owner": "cppcheck",
                "pattern": [
                    {
                        "regexp": "^(.*):(\\d+):(\\w+):(\\w+):(.*)$",
                        "file": 1,
                        "line": 2,
                        "severity": 3,
                        "code": 4,
                        "message": 5
                    }
                ]
            }
        }
    ]
}
```

然后在VSCode中按 `Ctrl+Shift+P` → 输入 `Run Task` → 选择 `static-check` 即可运行。Cppcheck会自动识别内存泄漏、空指针引用、数组越界等问题。

### 步骤3：设置Ceedling单元测试环境（测试核心算法）

Ceedling可以让你在PC上直接测试学生的C代码逻辑，无需烧录到硬件：

```bash
# 1. 安装Ceedling（需要Ruby环境）
gem install ceedling

# 2. 创建测试项目
ceedling new MyTest

# 3. 将学生代码放入 src/ 目录
# 4. 在 test/ 目录编写测试用例（以test_开头）
# 5. 运行测试
ceedling test:all
```

一个简单的测试用例示例：

```c
// test/test_led_control.c
#include "unity.h"
#include "led_control.h"

void setUp(void) {}
void tearDown(void) {}

void test_led_toggle_should_work(void) {
    // 假设学生实现了led_toggle()函数
    led_init();
    led_off();
    TEST_ASSERT_EQUAL(0, led_get_state());
    
    led_toggle();
    TEST_ASSERT_EQUAL(1, led_get_state());  // 断言：LED应该点亮
}
```

Ceedling会自动编译并运行所有测试用例，生成绿色的PASS/红色的FAIL报告，并可生成HTML格式的代码覆盖率报告。

### 步骤4：集成QEMU进行仿真测试（可选，进阶）

对于涉及硬件外设（如GPIO控制LED）的作业，可以使用QEMU模拟STM32F407来测试：

```bash
# 启动QEMU模拟STM32F407
qemu-system-arm -machine stm32f407-disc1 -cpu cortex-m4 \
                -nographic -semihosting \
                -kernel student_firmware.elf
```

QEMU会模拟真实的STM32硬件行为，包括GPIO、USART、Timer等外设，你可以在CI中自动化地注入测试数据并捕获输出。

---

## 📁 阶段三：全自动批阅——接入CI（半天可完成配置）

### 步骤1：搭建硬件测试台（如使用BenchCI）

```bash
# 安装BenchCI
pip install benchci

# 登录（可免费注册）
benchci login

# 执行测试套件
benchci run -b bench.yaml -s suite.yaml -a student_firmware.elf
```

编写测试用例 `suite.yaml`，自动判断LED亮灭等功能是否实现：

```yaml
version: "1"
suite:
  name: led_control_test
  tests:
    - name: "LED点亮测试"
      steps:
        - send_uart: { data: "LED_ON\n" }
        - delay_ms: 500
        - expect_uart: { contains: "LED已点亮", within_ms: 1000 }
        - expect_gpio: { pin: "PA5", value: "HIGH" }   # 直接检查引脚电平

    - name: "LED熄灭测试"
      steps:
        - send_uart: { data: "LED_OFF\n" }
        - delay_ms: 500
        - expect_gpio: { pin: "PA5", value: "LOW" }
```

BenchCI会自动完成烧录→执行测试→返回结果的全过程，支持UART交互和GPIO电平检测。

> 📌 **开源说明**：上述所有工具中，GCC、Cppcheck、QEMU、OpenOCD、Unity/Ceedling均为开源软件；BenchCI平台可免费注册使用。

---

## 📊 综合评分建议

建议采用以下多维度加权评分模型：

| 评分项 | 权重 | 评估方式 |
| :--- | :--- | :--- |
| 编译成功率 | 10-15% | 阶段一的批量编译脚本自动判断 |
| 代码规范 | 15-20% | Cppcheck报告的警告级别自动扣分 |
| 单元测试通过率 | 20-30% | Ceedling运行结果，按通过比例计分 |
| 仿真测试通过率 | 30-35% | QEMU + 测试脚本运行结果 |
| 硬件功能验证 | 15-20% | BenchCI硬件测试结果（可选加分项） |

---

## ✅ 快速开始建议

如果你是第一次尝试自动化批阅，建议按以下顺序推进：

1. **第1周**：部署阶段一（批量编译检查），投入15分钟即可解放编译问题筛选
2. **第2周**：引入Cppcheck静态检查，花1小时配置即可开始自动检测代码规范
3. **第1个月**：尝试Ceedling单元测试，针对算法类作业编写少量测试用例
4. **后续**：视需求逐步引入QEMU仿真和BenchCI硬件测试

如果想把学生环境也统一起来，后续可以考虑用 `project-generator` 来统一大家的环境。如果在搭建过程中遇到具体问题，随时可以再来问我～

---

## 🎯 基于STM32F407项目的适配方案

> 本章节针对STM32F407教学项目的实际情况，提供适配的自动化批阅实施方案。

### ✅ 可行性评估总结

| 原方案阶段 | 可行性 | 适配建议 |
|-----------|--------|----------|
| 阶段一：批量编译检查 | ⭐⭐⭐⭐☆ | 优先使用现有Makefile而非Keil |
| 阶段二：深度代码检查 | ⭐⭐⭐⭐⭐ | 项目已有完善的代码分析器 |
| 阶段三：CI+硬件测试 | ⭐⭐☆☆☆ | 成本高，建议暂缓 |

### 🏗️ 项目现有资源（已具备）

项目已经具备完整的自动化批阅基础设施：

| 资源 | 路径 | 功能 |
|------|------|------|
| **代码分析器** | `src/tools/plagiarism/code_analysis/code_analyzer.py` | 命名规范、复杂度、最佳实践、安全检查 |
| **评分引擎** | `src/tools/plagiarism/grading/grading.py` | 基于Rubric的自动评分 |
| **评分标准** | `data/rubrics/rubric.json` | 已定义7个评分类别 |
| **查重检测** | `src/tools/plagiarism/core/detector.py` | 多种相似度算法 |
| **安全工具** | `src/tools/security/` | ZIP解压、XML解析、路径验证 |
| **构建系统** | `Makefile` | GCC工具链，支持简单和CubeMX项目 |
| **报告生成** | `src/tools/plagiarism/report/report.py` | Excel/JSON/HTML报告 |

### 📋 实施方案（基于现有资源）

#### 新增模块

```
src/tools/auto_grading/
├── __init__.py        # 模块初始化 ✅ 已创建
├── config.py          # 配置管理 ✅ 已创建
├── build_checker.py   # 编译检查器（待实现）
├── grading_engine.py  # 整合评分引擎（待实现）
├── submission_processor.py  # 提交处理器（待实现）
└── facade.py          # 统一入口（待实现）
```

#### 实施优先级

**第一优先级**（核心功能）：
1. **BuildChecker** - 批量编译检查
   - 集成现有Makefile：`make PROJECT=<name> clean all`
   - 解析编译输出，提取错误信息
   - 生成编译检查报告

2. **SubmissionProcessor** - 学生作业处理
   - 复用 `zip_validator.py` 安全解压
   - 复用 `xml_parser.py` 提取报告内容
   - 识别项目文件结构

3. **AutoGradingEngine** - 整合评分引擎
   - 集成现有 `EnhancedCodeAnalyzer`
   - 集成现有 `RubricGrader`
   - 生成综合评分

**第二优先级**（增强功能）：
4. **AutoGradingFacade** - 统一入口
   - 端到端工作流
   - 进度追踪和日志
   - 批量处理支持

#### 评分权重建议（与rubric.json整合）

```json
{
  "categories": [
    {
      "id": "code_compilation",
      "name": "代码编译",
      "points": 10,
      "source": "automated",
      "criteria": [
        {"description": "项目编译通过无错误", "points": 10}
      ]
    },
    {
      "id": "code_quality",
      "name": "代码质量",
      "points": 20,
      "source": "automated",
      "criteria": [
        {"description": "命名规范符合要求", "points": 5},
        {"description": "代码复杂度控制良好", "points": 5},
        {"description": "遵循HAL最佳实践", "points": 5},
        {"description": "代码注释完整", "points": 5}
      ]
    }
  ]
}
```

### 📊 数据流设计

```
学生ZIP文件
    ↓
[SubmissionProcessor] 安全解压 → 提取报告 → 查找项目
    ↓
[BuildChecker] 编译检查 → 成功/失败 + 错误详情
    ↓
[EnhancedCodeAnalyzer] 代码分析 → 问题列表 + 质量分数
    ↓
[RubricGrader] 报告评分 → 关键词匹配得分
    ↓
[AutoGradingEngine] 综合评分 → 加权总分
    ↓
[ReportGenerator] 生成报告 → Excel多工作表
```

---

## 💡 关于学生提交格式的讨论

### 当前提交格式问题

**现有流程**：学生提交Word文档 → 提取代码块进行分析

**局限性**：
1. 代码块格式不统一，提取困难
2. 无法验证代码是否能实际编译运行
3. 缺少项目结构信息（头文件、配置等）
4. 难以进行完整的静态分析

### 建议的提交格式（更准确批阅）

#### 确定方案：双文件提交（已采用）

**原始提交格式**：
```
汽服2302B班-07-car-gear.zip              # 班级压缩包
├── 23071140101-张三.zip                 # 学生压缩包
│   ├── 实验报告.docx                   # WPS或Word格式
│   └── 源代码.zip                       # STM32CubeMX或Keil标准结构
├── 23071140102-李四.zip
│   ├── 实验报告.docx
│   └── 源代码.zip
└── ...
```

**处理后格式**（用于自动化批阅）：
```
data/teaching/2026-春季/汽服2302B班/07-car-gear/
├── reports/                             # 实验报告文件夹
│   ├── 汽服2302B班-23071140101-张三-实验报告.docx
│   ├── 汽服2302B班-23071140102-李四-实验报告.docx
│   └── ...
└── source/                              # 源代码文件夹
    ├── 汽服2302B班-23071140101-张三-源代码/
    │   ├── Core/Src/main.c
    │   ├── Core/Inc/main.h
    │   ├── Drivers/...
    │   └── MDK-ARM/                    # Keil项目文件
    ├── 汽服2302B班-23071140102-李四-源代码/
    │   └── ...
    └── ...
```

**文件命名规范**：
- 实验报告：`{班级}-{学号}-{姓名}-实验报告.{docx/doc}`
- 源代码文件夹：`{班级}-{学号}-{姓名}-源代码/`

**优势**：
- ✅ 格式统一，便于批量处理
- ✅ 报告和源代码分离，各自独立分析
- ✅ 源代码保持完整项目结构（CubeMX或Keil）
- ✅ 支持编译检查和完整静态分析
- ✅ 便于查重和对比

#### 源代码项目结构要求

**STM32CubeMX项目结构**：
```
汽服2302B班-23071140101-张三-源代码/
├── Core/
│   ├── Src/
│   │   ├── main.c
│   │   ├── stm32f4xx_it.c
│   │   └── ...
│   └── Inc/
│       ├── main.h
│       └── ...
├── Drivers/                            # HAL库
├── MDK-ARM/                            # Keil项目文件
│   ├── project.uvprojx
│   └── ...
└── ...
```

**Keil项目结构**（如果使用）：
```
汽服2302B班-23071140101-张三-源代码/
├── User/
│   ├── main.c
│   └── ...
├── Firmware/
│   └── ...
├── project.uvprojx
└── ...
```

#### 处理脚本设计

需要一个预处理脚本来实现格式转换：

```python
# src/tools/auto_grading/submission_organizer.py

class SubmissionOrganizer:
    """作业提交格式整理器"""

    def process_class_submission(self, class_zip: Path):
        """处理班级提交压缩包"""
        # 1. 解压班级压缩包
        # 2. 遍历学生压缩包
        # 3. 提取实验报告 -> reports/
        # 4. 解压源代码 -> source/
        # 5. 重命名文件和文件夹

    def extract_student_submission(self, student_zip: Path):
        """处理单个学生提交"""
        # 1. 解压学生压缩包
        # 2. 识别报告和代码文件
        # 3. 重命名并移动到对应目录
```

#### 方案B：单文件提交（简化版）

```
学号-姓名.zip
├── 实验报告.docx       # 包含完整代码
└── main.c              # 主程序文件（复制粘贴）
```

**优势**：
- ✅ 提交简单
- ✅ 可编译核心文件
- ⚠️ 缺少完整项目结构

#### 方案C：在线提交（理想方案）

使用Git或教学平台提交：
- 学生将代码推送到Git仓库
- 系统自动clone、编译、分析
- 报告作为附件上传

**优势**：
- ✅ 版本控制
- ✅ 自动化程度最高
- ✅ 可追踪修改历史
- ⚠️ 需要平台支持

### 实施建议

**短期（当前实验）**：
- 继续使用Word报告提交
- 要求学生将代码按标准格式放入报告
- 增强代码块提取算法

**中期（下学期）**：
- 采用方案A（双文件提交）
- 提交模板和说明文档
- 更新评分标准

**长期（未来）**：
- 考虑方案C（在线提交）
- 集成教学管理平台
- 实现完全自动化批阅

---

## 📝 总结

本方案的核心思路是：
1. **复用现有资源**，避免重复开发
2. **优先实现高价值功能**（编译检查）
3. **分阶段验证**，逐步完善
4. **考虑学生实际情况**，选择合适的提交格式

实施完成后，预计可将批阅效率提升50%以上，同时保证评分的一致性和准确性。